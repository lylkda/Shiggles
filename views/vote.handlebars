{{> usernavbar}}

  <main>
	<h1 class="header center cyan-text text-lighten-2" id="userQuestion">{{current-question}}</h2>
    <br>
    <br>
    <div class="container" id="loginBtn">
        <div class="center row">
            <div class="col s6">
                <a class="cyan waves-effect waves-light btn">{{vote-1}}</a>
            </div>
            <div class="col s6">
                <a class="cyan waves-effect waves-light btn">{{vote-2}}</a>
            </div>
        </div>
    </div>
  </main>

{{> footer}}
